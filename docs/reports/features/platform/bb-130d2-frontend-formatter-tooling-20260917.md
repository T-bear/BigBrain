# BB-130D2 — frontend formatter tool installation

## Metadata

- Baseline: `d7785b4ef2fea271cb78f0019c486b60acdad210`.
- Branch: `bb-130d/frontend-formatter-tooling`.
- Implementation/validation: 2026-09-16; interrupted-run recovery/publication: 2026-09-17.
- Detta är en sanerad GitHub-version. No secrets, private addresses or raw sensitive logs.
- Contract: [accepted characterization](bb-130d2-frontend-quality-characterization-20260914.md).
- Prior [dependency maintenance](bb-130d2-frontend-dependency-maintenance-20260916.md) remains accepted.

## Status

**ACCEPTED / MERGED / CI VERIFIED.** Tool contract only; publication amendment 2026-09-17.
Owner/architect approved exact candidate `d71ab8553871d9c5eb89c151ad8cb0392e0de08a`,
merged unchanged as `ba0037ee41f16476e74037f090e83e2e3a78f205`.
[Merge CI 35190196135](https://github.com/T-bear/BigBrain/actions/runs/35190196135): SUCCESS
for backend/frontend/documentation/secrets. Backend restore → actual dotnet format check →
Release build → tests passed. Frontend npm ci/test/build passed; no frontend formatter CI gate.
Post-merge local npm ci/test/build/audit: exit 0, 199/199 tests, production build, audit zero.
Prettier 3.9.6 dev-only, config/scripts unchanged; exact scope 83, 80 nonconforming,
format:check exit 1 expected, output identical to characterization. No write formatting.

No write-mode formatter executed. Cleanup and frontend formatter CI gate NOT STARTED / NOT COMPLETE.
Lint deferred, no ESLint/Biome/Stylelint, plugins or EditorConfig added. Backend formatter CI remains
unchanged and enabled. A/B complete; C complete/exit approved; D in progress. D1/backend cleanup/gate
and D2 characterization/triage/dependency maintenance remain accepted.

## Changes

Only package.json, package-lock.json, new Web .prettierrc.json and seven documentation files.
`npm install --save-dev --save-exact prettier@3.9.6 --ignore-scripts`: exit 0, one added package.
Prettier is exactly 3.9.6, dev-only. Complete lock review: only root devDependency entry and
node_modules/prettier added; all existing package records byte-equivalent as JSON, no upgrades,
new runtime dependencies or overrides. Registry URL/integrity generated by npm. Installed Prettier
manifest has no dependencies or lifecycle scripts. npm's incidental manifest dependency ordering
was returned to its original order; versions unchanged. Node v20.19.2 / npm 9.2.0.

Exact .prettierrc.json:

```json
{
  "singleQuote": true,
  "semi": false,
  "tabWidth": 2,
  "useTabs": false,
  "trailingComma": "all",
  "arrowParens": "avoid",
  "endOfLine": "lf",
  "jsxSingleQuote": false,
  "embeddedLanguageFormatting": "off",
  "printWidth": 120
}
```

Exact scripts from Web directory:

```json
{
  "format": "prettier --write --config .prettierrc.json --no-editorconfig \"src/**/*.{ts,tsx}\" vite.config.ts",
  "format:check": "prettier --check --config .prettierrc.json --no-editorconfig \"src/**/*.{ts,tsx}\" vite.config.ts"
}
```

`format` is provided for later separately authorized cleanup and was NOT executed. Explicit config
and --no-editorconfig match the accepted contract. No ignore policy needed for current scope.

## Evidence

### Exact scope proof

Git inventory has 83 tracked TS/TSX files: 82 under src and vite.config.ts (17 TS, 66 TSX),
including 23 TS/TSX test files. Filesystem expansion of src/**/*.ts plus src/**/*.tsx and
vite.config.ts equals this Git set exactly: no extra/missing/untracked files. CLI debug check
records 83 applied configurations and 80 warned TS/TSX paths. The three clean files are
src/main.tsx, src/test/setup.ts and vite.config.ts. Same result as accepted characterization.
Quoted glob is interpreted by Prettier, not shell expansion. It excludes CSS, the three JS
asset/layout tests, HTML/JSON/lockfiles/docs, dist/node_modules and other repository files.
Future additions under src with these extensions join scope; generated files must remain outside
this source tree. The glob is not a Git-tracked-only filter; inventory comparison is necessary
if untracked/generated TS is later introduced. Current exact scope has no such conflict.

Reproduce the set comparison from repository root without writing any files:

```python
from pathlib import Path
import subprocess
root = Path('src/BigBrain.Web')
tracked = {p for p in subprocess.check_output(['git', 'ls-files', str(root)], text=True).splitlines()
           if p.endswith(('.ts', '.tsx'))}
selected = {str(p) for ext in ['*.ts', '*.tsx'] for p in (root / 'src').rglob(ext)}
selected.add(str(root / 'vite.config.ts'))
assert selected == tracked and len(selected) == 83
```

### Completed validation and recovery

From src/BigBrain.Web on 2026-09-16:

| Command | Result |
| --- | --- |
| `npm ci` | exit 0, 117 installed / 118 audited; lock valid |
| `npm test -- --run` | exit 0, 199/199 tests, 26 files |
| `npm run build` | exit 0, TypeScript/Vite production build, 70 modules |
| `npm audit --json` | exit 0, zero findings |
| `npm run format:check` | exit 1, 80 nonconforming / 83 selected, formatting debt only |
| `npm run format:check -- --log-level debug` | exit 1, 83 CLI config applications, same 80 files, no parser/config error |
| Repeat `npm run format:check` after ci/test/build/audit | exit 1; output byte-identical to first check |

Expected nonzero check is characterization, not a failed installation or a green formatter baseline.
Existing jsdom media pause notices occur in passing tests. No write mode, source correction or
application refactor. Resumption on 2026-09-17 found the same branch/HEAD, package changes/config,
completed logs and unchanged main. No checkpoint commit or remote branch existed. The recovery
file still described the prior completed publication; no interruption note had been written.
This was missing current recovery information, not contradictory recorded formatter work.
State reconstructed from preserved files/results and session evidence; nothing reset/recreated.
The completed suites are reused under explicit resume instruction; no relevant source/package/
config changed since those commands. Only documentation follows; no expensive suite rerun solely
for reassurance. Repeat-output comparison and exact scope/source checks revalidated on resume.

`git diff HEAD --exit-code -- src ':!src/BigBrain.Web/package.json' ':!src/BigBrain.Web/package-lock.json' tests .github infrastructure`
returns 0 for tracked source/config outside package metadata. The sole new frontend file is
.prettierrc.json. All tracked TS/TSX/CSS/JS application/tests and Vite config remain byte-identical.
Backend source, workflow and deployment/runtime config unchanged. Frontend CI remains npm ci,
npm test -- --run, npm run build; no new gate. No local backend suite needed for tooling-only change.

## Security

Audit zero is dated registry evidence, not blanket security approval. No new advisory or blocker
established. No npm audit fix, additional dependency maintenance, deployment, runtime/device/UX
verification or behavior change. Finance RESEARCH / 0 SEK / NONE; no provider/broker/orders/
PAPER/LIVE/AUTO/capital, Research Learning, scientific semantics, identities/checksums,
entitlement/fail-closed or NOT EVALUABLE changes.

## Remaining work

Tooling accepted on main. Next checkpoint: **BB-130D2 — Frontend Formatter Cleanup**, requiring
separate owner/architect authorization. Plan bounded
frontend formatter cleanup batches with semantic/JSX review and existing regression tests.
Only after accepted full-scope cleanup should another checkpoint enable CI. Lint remains deferred.
No cleanup starts here. Rollback if separately authorized: revert tooling manifest/lock/config
and scripts together through reviewed Git history, npm ci/test/build; no source rollback needed.
README, ARCHITECTURE/ADRs, ROADMAP, modules, knowledge/index and runbooks assessed: unchanged
contracts/runtime/architecture need no edits. Catalog and canonical status/test/plan/recovery
are updated; historical reports preserved.

## Resumption

Read AGENTS/START-HERE and sole recovery note; preserve unrelated mockups/ADR 0006–0009.
Verify final reconciliation main and exact-head CI using the canonical recovery note. Do not
merge again or recreate commits. After publication STOP; no cleanup or CI activation authorized.

### Publication checks — 2026-09-17

Documentation verifier exit 0: 242 Markdown files / 90 BB IDs. Diff check exit 0.
Gitleaks v8.28.0 `git --log-opts='--all' --redact --no-banner`: exit 0, 275 commits, no leaks.
Prepublication fetch confirms unchanged main. Ten intended files only: three tooling/package
files and seven docs. Candidate remote SHA must match HEAD; no merge/CI success claimed.

`git diff --cached --check` and Gitleaks `git --pre-commit --staged --redact --no-banner`:
exit 0, no leaks. All ten staged files match the bounded scope.

### Accepted publication reconciliation — 2026-09-17

The merge tree equals the approved candidate tree. Fresh local verification on exact merge-main:
`npm ci`, `npm test -- --run`, `npm run build`, `npm audit --json` all exit 0; 199/199 tests
in 26 files, 70 transformed modules, zero audit findings. `npm run format:check` exits 1
solely for 80 existing differences; output byte-identical to candidate. Scope set comparison
above again proves exactly 83 tracked intended files. Installed package/lock verify 3.9.6 dev-only;
accepted config and scripts above unchanged. No overrides, unrelated updates or lint tooling.
Source/tests/backend/workflow/runtime comparison to original baseline exits 0 excluding only
the three approved tooling/package files. No write formatter command executed. No deployment.
Only seven canonical docs reconciled; historical implementation/recovery evidence above retained.
README, ARCHITECTURE/ADRs, ROADMAP, modules, knowledge/index and runbooks reassessed: no changed
architecture/contracts/runtime need edits. Finance RESEARCH / 0 SEK / NONE; no scientific change.
Final reconciliation commit requires its own exact-head CI with all four jobs and actual backend
formatter execution. Resolve its SHA/run from GitHub history using recovery instructions;
own future SHA is not embedded recursively. No frontend formatter gate enabled.

Reconciliation publication checks: documentation verifier exit 0 (242 Markdown files / 90 BB IDs);
working/staged diff checks exit 0; Gitleaks v8.28.0 full-history exit 0 (276 commits) and staged
check exit 0, no leaks. Seven docs only; source/tests/CI/runtime unchanged. Prepublication fetch
confirms exact merge-main and unchanged approved candidate. Final exact-main CI still required.
