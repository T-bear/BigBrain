import { useEffect,useState } from 'react'
import { getAudiobookLanguages,setAudiobookLanguages } from '../api'
import { BBSelect } from '../components'
export function AudiobookSettings(){const[preferred,setPreferred]=useState('sv');const[fallback,setFallback]=useState('en');const[status,setStatus]=useState('');useEffect(()=>{void getAudiobookLanguages().then(v=>{setPreferred(v.preferredLanguage);setFallback(v.fallbackLanguage)}).catch(()=>setStatus('Inställningen kunde inte laddas.'))},[]);async function save(p:string,f:string){setStatus('Sparar…');try{const v=await setAudiobookLanguages(p,f);setPreferred(v.preferredLanguage);setFallback(v.fallbackLanguage);setStatus('Sparat')}catch{setStatus('Inställningen kunde inte sparas.')}}return <fieldset className="audiobook-language-settings"><legend>Ljudboksspråk</legend><label>Föredraget språk<BBSelect value={preferred} onChange={e=>void save(e.target.value,fallback)}><option value="sv">Svenska</option><option value="en">Engelska</option><option value="de">Tyska</option></BBSelect></label><label>Reservspråk<BBSelect value={fallback} onChange={e=>void save(preferred,e.target.value)}><option value="en">Engelska</option><option value="sv">Svenska</option><option value="de">Tyska</option></BBSelect></label><small aria-live="polite">{status}</small></fieldset>}
